# MUPSA görev geçmişi

- 2026-08-27: PWA/Web Push güvenilirlik ve güvenlik sertleştirmesi hazırlandı. Cihaz bazlı teslim/claim/retry modeli, 28 günlük TTL, uzun backoff, logout güvenliği, notification RLS/column privilege daraltması, push subscription gizliliği, VAPID env zorunluluğu, SW click doğrulaması ve Unicode payload kısaltması eklendi. Gerçek Supabase deploy/push ve Git commit/push yapılmadı. npm lint/build, git diff --check, Supabase linked lint + db push dry-run ve Trivy çalıştırıldı; Deno CLI bulunmadığı için Edge Function `deno check` çalıştırılamadı.
- 2026-08-27: Bağımsız denetim sonrası logout push temizliği tek merkezde toplandı; push TTL/retry/error sınıflandırması/URL/payload kuralları ortak test edilebilir modüle çıkarıldı ve 5 otomatik `npm run test:push` testi eklendi. Lint, build, git diff check, Supabase linked lint + dry-run ve Trivy yeniden geçti. Vercel public VAPID env doğrulaması oturum gerektirdiği için beklemede; deploy/commit/push yapılmadı.
- 2026-08-27: Vercel Production ortamına `VITE_WEB_PUSH_PUBLIC_KEY` eklendi. `20260827231000_harden_web_push_delivery.sql` linked Supabase veritabanına uygulandı ve migration listesinde local/remote eşleşmesi doğrulandı. `deliver-push-notifications` Edge Function ACTIVE version 12 olarak deploy edildi. Git commit/push tamamlandı; Vercel canlı adresi `https://mupsa-ekip-koordinasyon.vercel.app` üzerinde HTTP 200 ve login ekranıyla doğrulandı.
- 2026-08-28: Takvim ay başlığı altındaki etkinlik/görev/farkındalık sayaçları dönem toplamı yerine seçili ayla kesişen benzersiz kayıtları gösterecek şekilde düzeltildi. Lint, production build ve git diff check geçti; değişiklik `main` dalına push edilmek üzere hazırlandı.
- 2026-08-28: Etkinlik ve farkındalık hazırlık başlangıç tarihleri manuel seçime çevrildi; mevcut kayıt tarihleri korunurken yeni/güncellenen kayıtların hazırlık tarihini otomatik ezme durduruldu. Supabase linked dry-run ve migration push başarılı oldu. Doğrulama sırasında önceki push claim fonksiyonundaki PL/pgSQL değişken/kolon belirsizliği de ayrı migration ile düzeltildi; linked DB lint ardından `No schema errors found` verdi. Frontend lint/build/diff-check geçti.
- 2026-08-28: Etkinliklere ortak koordinatör sistemi eklendi. Ana sahip korunurken birden fazla ortak koordinatör atanabiliyor; ortak koordinatörler etkinlik genel alanları ve bağlı görevleri yönetebiliyor, fakat ana sahiplik ve ortak koordinatör listesi yalnız ana sahip/Süper Yönetici tarafından değiştirilebiliyor. Üst koordinatör filtresi ortak etkinlikleri ilgili tüm koordinatörlüklerde tekil olarak gösteriyor. `20260828220000_add_event_coordinators.sql` linked Supabase'e uygulandı; migration listesi ve DB lint temiz. Frontend lint/build/diff-check geçti. Commit `95232fb` main'e pushlandı; bağlı Chrome DOM okuması takıldığı için canlı form doğrulaması bu turda tamamlanamadı ve test verisi oluşturulmadı.
- 2026-08-28: Ortak koordinatör seçim UX'i sadeleştirildi. Yeni etkinlik formuna varsayılanı `Hayır` olan "Ortak koordinatör var mı?" Evet/Hayır seçimi eklendi; yalnız Evet seçilince ortak koordinatör listesi açılıyor. Seçeneklerde koordinatörlük adı ve kişi adı birlikte gösteriliyor; Evet seçiliyken en az bir kişi seçimi zorunlu. Mevcut etkinlik detayındaki ana/ortak koordinatör kartları ve ekleme seçimi de koordinatörlük + isim sırasına geçirildi. Lint/build/diff-check temiz.
- 2026-08-28: Etkinlikler sayfasına arama alanı ve `Ortak etkinlik` rozeti eklendi. Arama başlık, açıklama, ana koordinatör adı, ana koordinatörlük ve ortak koordinatörlük adlarında çalışıyor; mevcut koordinatör filtresiyle birlikte uygulanıyor. Sonuç sayısı, aramayı temizleme ve boş sonuçta filtreleri temizleme akışı eklendi. En az bir ortak koordinatörü olan kartta ortak etkinlik rozeti mobil ve masaüstünde gösteriliyor. Lint/build/diff-check temiz; Git push sırada.
- 2026-08-28: Obsidian MUPSA kanonik notlarının görünen başlıkları/aliasları sadeleştirildi; 00–03 ana notları karşılıklı çapraz bağlandı. GPT-Memory sistem notlarında fiziksel dosya adı yeniden adlandırma Bridge tarafından engellendiği için dosya adları değişmedi. `GUNLUK.md` içindeki checkpoint bağlantıları ve 00–03 wiki-link hedefleri doğrulandı.
- 2026-08-28: Kullanıcının kastettiği tarih/hash ağırlıklı checkpoint dosya adlarını fiziksel olarak yeniden adlandırma denendi ancak GPT-Memory sistem notu güvenlik kuralı nedeniyle engellendi. Bunun yerine `Kontrol-Noktalari/00-OKUNUR-INDEKS.md` oluşturuldu, `GUNLUK.md` bağlantıları okunur başlıklarla etiketlendi ve ana başlangıç notuna indeks bağlantısı eklendi. Aktif `note/` ve `decision/` klasörlerinin boş olduğu, indekslenen checkpoint hedeflerinin mevcut olduğu doğrulandı.
- 2026-08-28: Etkinlik düzenleme regresyonu incelendi. Git geçmişi hazırlık başlangıç tarihinin `1ccf721` ile 2026-08-09'da otomatik/disabled yapıldığını, 2026-08-28 manuel tarih düzeltmesinde mevcut etkinlik düzenleme ekranının atlandığını gösterdi. `EventDetail.tsx` içinde hazırlık tarihi tekrar manuel/kaydedilebilir yapıldı. Süper yönetici görünürlüğü ayrıca DB'nin `is_super_admin()` RPC sonucu ile doğrulanacak şekilde `useMembershipStatus.ts` güçlendirildi; frontend ve veritabanı yetki kaynağı eşitlendi. Lint/build/diff-check temiz.
- 2026-08-29: Etkinlik Genel Bakış UI aşama 1 tamamlandı. `Tarihleri düzenle` tam etkinlik formu yerine yalnız dört tarihi yöneten odaklı modal açıyor; Tarihler kartından süreç/rapor bilgileri çıkarıldı. Genel Bakışa ayrı `Süreçler` kartı eklendi; SKS gerçek durumuyla, mevcut birleşik Tasarım/Duyuru durumu ayrı satırda gösteriliyor. Tasarım/Duyuru değişikliği anlık dropdown yerine odaklı düzenleme modalından kaydediliyor. Sonraki işlem ve Mekân mevcut özel hızlı düzenleme pencerelerini koruyor. Eski otomatik hazırlık açıklaması manuel davranışla uyumlu hale getirildi. Veri modeli/migration değiştirilmedi. Lint 0/0, production build ve git diff check geçti.
- 2026-08-29: Etkinlik süreç ayrımı aşama 2 uygulandı. Genel Bakış Süreçler kartından SKS çıkarıldı; SKS yönetimi Operasyon'da kalıyor. Tasarım ve Duyuru/Yayın gerçek veri alanları olarak ayrıldı. Yeni `event_announcement_statuses` tablosu ve `events.announcement_status` alanı eklendi. Tasarım seçenekleri Gerekli Değil / Brief Bekliyor / Tasarımda / Revizede / Hazır; Duyuru/Yayın seçenekleri Gerekli Değil / İçerik Bekliyor / Yayın Planlandı / Yayınlandı. Eski birleşik veriler migration ile taşındı. Tasarım sahibi yalnız Tasarım durumunu, Basın/Yayın sahibi yalnız Duyuru/Yayın durumunu yönetiyor; Süper Yönetici ikisini de yönetebiliyor. Bütçe erişimi değiştirilmedi. Linked Supabase migration push ve DB lint temiz; frontend lint/build temiz.
- 2026-08-29: Süreç ayrımı sonrası tüm etkinlik detaylarının genel yükleme hatasına düşmesi için hotfix hazırlandı. Canlı Supabase şemasında yeni `announcement_status` alanı/tablosu doğrulandı. Ana etkinlik yükleme sorgusu ikincil Duyuru/Yayın alanından ayrıldı; Duyuru/Yayın durumu ayrı ve hataya dayanıklı olarak yükleniyor. Yetkili bütçe RPC hatası da artık bütün etkinlik sayfasını çökertmiyor. Lint/build/diff-check temiz.
- 2026-08-29: Etkinlik detay kalan UI tamamlamaları yapıldı. Genel Bakış üst kartına ana yaşam döngüsü (Fikir → Planlanıyor → Kesinleşti → Gerçekleşti → Raporlandı → Arşivlendi) eklendi; Ertelendi/İptal ayrı rozetle gösteriliyor. Süper Yönetici sorumlu seçiminde rol/koordinatörlük + isim birlikte gösteriliyor. Operasyon SKS kartı kapalıyken mevcut SKS durum rozeti görünür hale getirildi. Bütçe görünürlüğünün yalnız Süper Yönetici/Sayman için frontend + DB tarafında zaten kısıtlı olduğu doğrulandı ve genişletilmedi. `announcement_status` yeni kolonu için kolon bazlı SELECT grant migration'ı eklendi ve linked Supabase'e uygulandı; böylece Duyuru/Yayın gerçek değeri okunabilir. Lint/build/diff-check ve linked DB lint temiz. Commit `feca2bf` main'e pushlandı; Vercel sonrası bağlı Chrome inspect işi takıldığı için canlı DOM son kontrolü tamamlanamadı ve hiçbir canlı veri değiştirilmedi.
