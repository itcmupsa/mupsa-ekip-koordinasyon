# AI devir notu — Tema, PWA duyurusu ve Antigravity

Son güncelleme: 13 Eylül 2026, Europe/Istanbul. Bu belge sohbetler arası devam için hazırlanmıştır. Yerel dal ve dosyalar bu not yazılırken okundu; önceki test ve canlı sonuçları ayrıca belirtilmiştir. Yeni ajan önce `git status` ve uzak/yayın durumunu tekrar kontrol etmelidir.

## Hızlı durum

| İş | Durum |
| --- | --- |
| Takvimler merkezi, ayrı Etkinlik/Farkındalık/PR takvimleri | Önceki oturumda canlıya alındı |
| PR bağlantı adı ve birden fazla paylaşım kanalı | Önceki oturumda canlıya alındı; son bilinen yayın `c52bf95` |
| Kullanıcıya özel tema rengi | Uygulama ve testler tamamlandı; frontend yayını/canlı kontrol bekliyor |
| Tema özelliği için PWA duyurusu | Migration'lar uygulandı ve rollback testi geçti; canlı frontend doğrulaması sonrası gerçek kuyruklama bekliyor |
| Antigravity MCP model seçimi | Eklendi, yerel eklenti yeniden kuruldu, Pro bağlantı testi geçti |
| PR onay mekanizması | Kullanıcı iş akışını sonra netleştirecek; uygulanmayacak |

Tema migration'ları bağlı veritabanına kalıcı olarak uygulandı; tema kodu henüz commit/push edilmedi ve tema duyurusu gönderilmedi.

## Proje ve çalışma biçimi

- Çalışma alanı: `/Users/numanondes/Desktop/websiteuygulama`.
- Uygulama deposu: `/Users/numanondes/Desktop/websiteuygulama/mupsa-repo`.
- Git uzak deposu: `https://github.com/itcmupsa/mupsa-ekip-koordinasyon.git`.
- Canlı uygulama: `https://mupsa-ekip-koordinasyon.vercel.app`.
- Aktif dal: `feature/user-theme-pwa-announcement-20260912`.
- Yerel HEAD: `c52bf95` — `release(pr): baglanti adlari ve coklu kanal secimini yayinla`.
- Kullanıcı son talebinde kalan tema/PWA işlerinin tamamını ana ajanın tek akışta bitirmesini istedi. Ayrı, anlaşılır isimli dal ve geri dönüş imkânı korunmalı. İncelemeden yayınlama.
- Mevcut commit edilmemiş değişiklikleri koru. Aşağıdaki geliştirme dosyaları önceki ajanların çalışmasıdır; yeniden sıfırdan başlama veya üzerine kontrolsüz yazma.
- Eski alt ajanların bir kısmı kullanım sınırına ulaştı. İsimlerinin görünmesi aktif çalıştıkları anlamına gelmez; görev vermeden durumlarını kontrol et.

## Yereldeki tema çalışması

Başlıca değişen dosyalar:

- `src/App.tsx`, `src/index.css`, `src/pages/AccountSettings.tsx`, `tailwind.config.js` ve dinamik marka yüzeyi kullanan bileşenler.
- Yeni: `src/components/ThemeProvider.tsx`, `src/components/themeContext.ts`, `src/lib/theme.ts`, `src/lib/authRedirect.ts`, `tests/theme.test.ts`, `tests/authRedirect.test.ts`.
- Yeni: `supabase/migrations/20260912120000_user_theme_preferences.sql`.
- Yeni: `supabase/tests/user_theme_preferences_rollback.sql`, `supabase/tests/user_theme_preferences_scenarios.md`.

Amaç ve mevcut yaklaşım:

- Ayarlar → Görünüm'de hazır renkler, özel renk seçici, HEX alanı, kaydetme ve varsayılana dönüş.
- Renk hesaba ait; `user_theme_preferences` tablosunda saklanıyor. Başka kullanıcının tercihini değiştirmemeli.
- Mevcut yeşil varsayılan korunuyor. Ortak CSS renk değişkenleri güncelleniyor; anlam taşıyan durum renklerinin davranışı incelemede korunmalı.
- Hesap değişiminde eski isteğin yeni hesabın temasını uygulamaması için kimlik ve nesil kontrolü eklendi; tarayıcıda ayrıca doğrulanmalı.
- Açık renkler okunabilirlik için işleniyor. Statik PWA manifesti ortak kalır; çalışma anında arayüz ve `meta theme-color` değişir. Kullanıcıya ikon/açılış ekranının da kişiselleştiği söylenmemeli.

13 Eylül 2026 doğrulama sonuçları:

- `npm run lint` ve `npm run build` temiz geçti; Fast Refresh uyarısı giderildi.
- Tema ve güvenli derin bağlantı testleri 7/7; PR takvim testleri 4/4; push testleri 5/5 geçti.
- Tema RLS ve sürüm duyurusu SQL senaryoları bağlı veritabanında kendi fixture'larıyla transaction + rollback içinde geçti.
- `20260912120000_user_theme_preferences.sql`, `20260912121000_theme_release_announcement.sql`, varsayılan execute yetkilerini sıkılaştıran `20260912122000_lock_theme_release_announcement.sql` ve eşzamanlı farklı dönem çağrısını açıkça reddeden `20260912123000_guard_theme_release_concurrent_period.sql` bağlı veritabanına uygulandı. Son dry-run veritabanını güncel gösterdi.
- Yerel tarayıcı görsel kontrolü `.env.local` içinde Supabase public değişkenleri olmadığı için uygulama başlamadan durdu. Üretim ortamı kontrolü frontend yayınından sonra yapılmalı.

## PWA duyurusu: tamamlanan güvenlik ve hedefleme

Taslak dosyalar:

- `supabase/migrations/20260912121000_theme_release_announcement.sql`.
- `supabase/tests/theme_release_announcement_rollback.sql`.
- [Tema duyuru çalıştırma taslağı](TEMA_SURUM_DUYURUSU.md).

`brand_notification_as_mupi()` içindeki geniş `release_key` istisnası kaldırıldı; duyuru mevcut MUPİ markalamasını kullanıyor. `queue_theme_release_announcement(target_period_id uuid)` hedef dönemi açıkça alıyor; null, bulunamayan, pasif ve alıcısız dönemleri reddediyor. Aynı sürüm anahtarını farklı döneme sessizce yönlendirmiyor. Execute yetkisi `public`, `anon` ve `authenticated` rollerinden açıkça alındı, yalnız `service_role` için verildi.

Korunacak davranış:

- Migration kurulumu kendi başına bildirim göndermemeli.
- Tek sürüm anahtarı: `user-theme-pwa-announcement-20260912`. Yeniden/eşzamanlı çağrı çift bildirim oluşturmamalı.
- Yalnız hedef dönemin aktif üyeliği ve aktif profili olan alıcılar.
- `service_role` yürütme yetkisi; tarayıcıdan ve normal authenticated kullanıcıdan başlatılamaz.
- `in_app` kaydından mevcut push tetikleyicisi ve teslim mekanizması kullanılır. E-posta gönderilmez; mevcut abonelik/izin davranışı korunur.
- Hedef: `/app/ayarlar?section=appearance`. Bildirime tıklayınca Görünüm alanı gerçekten açılmalı; oturum kapalıysa giriş sonrası yönlendirme de kontrol edilmeli.
- Kullanıcı yeni özelliğin PWA ile duyurulmasını istedi. Yalnız canlı özellik ve hedef alıcılar doğrulandıktan sonra gönder. Tekrarları önleyen kayıtları silerek yeniden gönderim yapma.

## Kalan yayın planı

1. İncelenmiş değişiklikleri dalda commit edip uzak dala ve ardından güncel `main` üzerine yayınla.
2. Vercel canlı sürümünde `/app/ayarlar?section=appearance` hedefini ve giriş dönüş parametresini doğrula. Kimlik bilgisi olmadan hesap içi kaydet/yenile testi yapılamazsa bunu açıkça raporla.
3. Aktif dönem sorgusunu çalıştır. Tek ve açık hedef varsa, daha önce kuyruklanmadığını doğrulayıp duyuruyu bir kez çalıştır; dönem belirsizse kullanıcı kararı olmadan gönderme.
4. Kuyruk ve teslim durumlarını ayrı raporla; gerçek cihaz kanıtı olmadan “ulaştı” deme. Yayın commit'i ve sonuçları bu belgelere işle.

Geri dönüş: yeni yayın commit'leri oluşunca gerçek commit kimlikleriyle revert planı yaz. Önceki bilinen frontend `c52bf95`; kullanıcı verisi taşıyan additive tabloları geri dönüş için silme. Gönderilmiş push bildirimi geri çekilmiş sayılamaz.

## Takvim kararları ve sonraya bırakılanlar

- Takvimler girişinde Etkinlik, Farkındalık ve PR ayrımı; farkındalık haftaları etkinlik görünümünü kaplamamalı.
- PR'de haftalık görünüm ana planlama alanı, aylık görünüm ek seçenek. Bir günde çok kayıt, kullanışlı detay/düzenleme paneli, mobil kullanım ve isteğe bağlı saat korunmalı.
- Manuel kayıtlar seçilen üç takvim kapsamında gösterilebilir; kaynak/farkındalık/görev bağlantıları bozulmamalı.
- Kullanıcı kayıt renklerini seçebiliyor. PR paylaşım/çekim türleri ile durum ve koordinatör sorumlulukları birbirine karıştırılmamalı.
- Harici bağlantı adı ve çoklu kanal kutuları önceki sürümde tamamlandı; yeniden geliştirme işi değildir.
- Onay mekanizması için kullanıcı daha sonra kuralları verecek; şimdiden rol/iş akışı uydurma.
- Önceki takvim incelemesinde eski audit bütçe görünürlüğü için açık not bulunuyor; bu tema çalışmasıyla giderilmiş sayılmamalı. İncelemek gerekirse ilgili raporu ve güncel SQL yetkilerini ayrıca doğrula.
- Referanslar: [Takvim kontrolü](CALENDAR_PR_REVIEW_CHECKPOINT.md), [veri sözleşmesi](CALENDAR_PR_DATA_CONTRACT.md), [AI durumu](AI_DURUM.md), [kalıcı proje bağlamı](AI_BAGLAM.md).

## Antigravity: tamamlanan yerel araç güncellemesi

- AGY CLI `1.2.2`. MCP `agy_setup`, `agy_review`, `agy_adversarial_review`, `agy_rescue` artık isteğe bağlı `model` parametresi alıyor; companion/runtime üzerinden CLI `--model` argümanına geçiyor. Model verilmezse CLI varsayılanı korunuyor.
- Doğrulanan seçim: `gemini-3.1-pro-high`.
- Model kimliği doğrulaması ve arka plan işinde modelin saklanması test edildi. MCP varsayılanlarında otomatik `--dangerously-skip-permissions` kaldırıldı; açık CLI seçeneği ayrı kaldı. Çalışma izni hatalarını bu bayrakla sessizce aşma.
- Terra uyguladı; ana ajan farkı inceledi. Tam paket **61/61 geçti**, plugin doğrulaması ve diff whitespace kontrolü geçti.
- Kurulu MCP sunucusundan gerçek `agy_setup` çağrısı (`smoke: true`, `timeout: "30s"`, `model: "gemini-3.1-pro-high"`) başarılı: `AGY ready: yes`, `print smoke: pass`. Yalnız sabit test cümlesi gönderildi; MUPSA kaynak kodu gönderilmedi.
- Yeni MCP şemasını görmek için yeni Codex görevi açılmalı; mevcut görevin araç tanımları kendiliğinden güncellenmiş varsayılmamalı.
- Kaynak: `/Users/numanondes/.codex/.tmp/marketplaces/antigravity-plugins/plugins/agy`.
- Kurulu cache: `/Users/numanondes/.codex/plugins/cache/antigravity-plugins/agy/0.4.0`.
- Codex manifest sürümü: `0.4.0+codex.20260912211200`. Cache klasör adının `0.4.0` kalması kurulum hatası değildi; kurulu sunucu şeması ayrıca doğrulandı.
- Düzenleme/test kopyası: `/Users/numanondes/Desktop/websiteuygulama/agy-model-support`.
- Kurulum öncesi yedek: `/Users/numanondes/Desktop/websiteuygulama/agy-install-backup/agy`.
- Yerel kurulum `codex plugin add agy@antigravity-plugins` ile yapıldı; eklenti upstream'ine Git yayını yapılmadı. Gelecek marketplace güncellemesi yerel yamayı üzerine yazabilir; model desteğini kontrol et.

Önemli yetki durumu: önceki özel MUPSA kaynak dosyalarını AGY/Gemini'ye aktarma denemesi otomatik onay incelemesinde reddedildi; gerekçe bu belirli kaynak içeriğinin dış servise aktarımı için açık onay bulunmamasıydı. Pro araç kurulumuna verilen onay, bu reddedilen kaynak aktarımının onayı sayılmamalı. AGY ile kaynak incelemesine geçerken gereken belirli aktarım onayını çöz; izin aşımı yapma. Yerel inceleme/test ve diğer bağımsız işleri sürdürebilirsin.

## Bu notu kullanacak sonraki ajana

Önce bu notu, ardından `docs/AI_DURUM.md` ve ilgili dosyaları oku. Tarihli “geçti” sonuçları geçmiş kanıttır; dosya değişmişse uygun testleri yeniden çalıştır. Bu notta anahtar, parola veya oturum bilgisi yoktur. Obsidian'a bu turda kopya yazılmadı; kalıcı devir konumu proje deposudur. Kullanıcıdan eski sohbeti tekrar anlatmasını istemeden, en güncel isteğinin kapsamında ilerle.
